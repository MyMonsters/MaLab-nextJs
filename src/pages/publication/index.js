import React, { memo } from 'react';
import { PublicationWrapper } from './style';

const Publication = memo(function Publication() {
  return <PublicationWrapper>Publication</PublicationWrapper>;
});
export default Publication;
